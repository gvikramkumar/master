import { IdpidValidatorDirective } from './idpid-validator.directive';

describe('IdpidValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IdpidValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
