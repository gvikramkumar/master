import { CustomRangeValidatorDirective } from './custom-range-validator.directive';

describe('CustomRangeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomRangeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
