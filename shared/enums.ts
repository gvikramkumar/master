
export enum Directory {
  businessUpload = 'prof.bu'
}

export enum BusinessUploadFileType {
  upload = 'upload',
  template = 'template'
}

export enum Modules {
  prof = 1,
  prdt,
  bkgm,
  svct,
  tsct,
  ascg,
  cisc,
  opex,
  defr,
  gubr,
  bkir,
  rrev,
  admn = 99
}
