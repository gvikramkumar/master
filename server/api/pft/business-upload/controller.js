const BusinessUploadRepo = require('./repo'),
  schema = require('./schema');

const repo = new BusinessUploadRepo();

module.exports = class BusinessUploadController {
  constructor() {
  }

  add() {

  }

}

