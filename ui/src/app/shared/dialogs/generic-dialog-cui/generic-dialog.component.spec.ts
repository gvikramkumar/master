/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { GenericDialogCuiComponent } from './generic-dialog.component';

describe('GenericDialogCuiComponent', () => {
  let component: GenericDialogCuiComponent;
  let fixture: ComponentFixture<GenericDialogCuiComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ GenericDialogCuiComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(GenericDialogCuiComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
