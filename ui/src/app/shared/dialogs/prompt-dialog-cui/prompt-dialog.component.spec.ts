/*
import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PromptDialogCuiComponent } from './prompt-dialog.component';

describe('GenericDialogComponent', () => {
  let component: PromptDialogCuiComponent;
  let fixture: ComponentFixture<PromptDialogCuiComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PromptDialogCuiComponent ]
    })
      .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PromptDialogCuiComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
