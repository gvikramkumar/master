import { ErrorFlashDirective } from './error-flash.directive';

/*
describe('ErrorFlashDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorFlashDirective();
    expect(directive).toBeTruthy();
  });
});
*/
