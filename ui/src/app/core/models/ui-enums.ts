
export enum DialogType {
  ok = 1,
  okCancel,
  yesNo
}

export enum DialogInputType {
  input = 1,
  textarea
}

export enum DialogSize {
  small = '500px',
  medium = '600px',
  large = '700px'
}

