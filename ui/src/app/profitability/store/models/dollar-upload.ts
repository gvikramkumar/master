import {ModelBase} from '../../../store/models/model-base';

export class DollarUpload extends ModelBase {
  createdBy?: string;
  createdDate?: string;
  updatedBy?: string;
  updatedDate?: string;
}
