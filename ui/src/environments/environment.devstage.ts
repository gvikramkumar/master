export const environment = {
  production: true,
  apiUrl: '',
  disableAnimations: false,
  // this is only difference between dev/stage and prod. We don't show verbose error messages in prod, but we need them for bugs in dev/qa
  showVerboseErrorMessages : true
};
